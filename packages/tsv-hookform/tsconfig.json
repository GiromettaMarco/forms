{
  "extends": "@gmcode/typescript-config/react.json",

  // Files
  "include": ["src", "tests"],
  "exclude": ["dist", "node_modules"],

  "compilerOptions": {
    // Paths
    "baseUrl": ".",
    "paths": {
      "@/*": ["src/*"]
    },

    // Types
    "types": ["vitest/jsdom"]
  }
}
